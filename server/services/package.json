{
  "name": "services",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest --collectCoverage --detectOpenHandles --forceExit --runInBand --verbose --watchAll",
    "migration": "npm run migration --prefix ./user && npm run migration --prefix ./restaurant && npm run migration --prefix ./food && npm run migration --prefix ./cart",
    "database-drop": "npm run database-drop --prefix ./user"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "jest": "^26.5.3"
  }
}
